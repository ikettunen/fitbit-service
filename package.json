{
  "name": "fit-bit-service",
  "version": "1.0.0",
  "description": "Microservice to fetch Fitbit data for patients and manage API key links",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:report": "jest --coverage && node scripts/generate-report.js",
    "seed-demo": "node scripts/seed-demo-data.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "ioredis": "^5.4.1",
    "pino": "^8.15.0",
    "express-pino-logger": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.1.4",
    "jest-html-reporter": "^3.10.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
