# Fitbit Service deployment workflow for EC2 self-hosted runner
# Deploys Node.js service to EC2 and restarts with PM2

name: Deploy Fitbit Service to EC2

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js 22
      uses: actions/setup-node@v3
      with:
        node-version: '22.x'
        cache: 'npm'
    
    - name: Verify Node.js version
      run: |
        node --version
        npm --version
    
    - name: Install dependencies
      run: npm ci
    
    - name: Deploy to EC2
      run: |
        # Backup current version
        if [ -d ~/fitbit-service ]; then
          cp -r ~/fitbit-service ~/fitbit-service.backup
        fi
        
        # Copy new files (excluding node_modules and .git)
        rsync -av --exclude='node_modules' --exclude='.git' --exclude='logs' --exclude='data' ./ ~/fitbit-service/
        
        # Install dependencies in target directory
        cd ~/fitbit-service
        
        # Copy production environment as .env
        cp .env.production .env
        
        # Add deployment timestamp (cross-platform compatible)
        {
          echo ""
          echo "# Last deployed: $(date -u +'%Y-%m-%d %H:%M:%S UTC') via GitHub Actions"
          echo "# Deployment ID: $GITHUB_RUN_ID"
          echo "# Commit SHA: $GITHUB_SHA"
          echo "# Commit Message: $(git log -1 --pretty=format:'%s' $GITHUB_SHA 2>/dev/null || echo 'No commit message available')"
        } >> .env
        
        npm install --production
        
        echo "Deployed to ~/fitbit-service"
    
    - name: Restart service with PM2
      run: |
        cd ~/fitbit-service
        pm2 restart fitbit-service || pm2 start src/server.js --name fitbit-service
        pm2 save
    
    - name: Deployment Summary
      run: |
        echo "✅ Fitbit Service deployed successfully!"
        echo "🌐 Service URL: http://51.20.164.143:3010"
        echo "📁 Deployed to: ~/fitbit-service"
        echo ""
        echo "PM2 Status:"
        pm2 list | grep fitbit-service
